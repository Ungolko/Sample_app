<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
<!-- Rails will receive User-class-object list and for each @user auto find _user.html.erb  view -->
  <%= render @users %>
  </ul>

<%= will_paginate %>
